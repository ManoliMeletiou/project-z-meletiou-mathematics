# AI Agent Continuation Prompt — Phase 52

Continue Project Z from the Phase 52 security and release foundation.

Start with:

- `PROJECT_Z_CURRENT_HANDOVER.md`
- `docs/handover/PROJECT_Z_HANDOVER_PHASE_52.md`
- `docs/PROJECT_Z_COMPLETION_MASTER_PLAN.md`

The Supabase production migration `phase_52_secure_function_execution` is already applied. Do not apply it a second time. Anonymous executable `SECURITY DEFINER` functions are now zero.

The local Phase 52 release still needs publication, GitHub CI, signed-in student/teacher/parent smoke tests, and Vercel production verification. The GitHub publish workflow was blocked in the prior environment because the required `gh` CLI was unavailable.

After the Phase 52 exit gate passes, continue with Phase 53: Assignment Factory Command Centre.

Never weaken RLS, reopen anonymous RPC execution, expose parent-unsafe information, or let AI-generated assignments bypass teacher-controlled audit.
